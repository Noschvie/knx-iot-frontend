# Gateway (KNX IoT Gateway) - runs independently in a separate stack
# Configure access via GATEWAY_URL, CLIENT_ID, and CLIENT_SECRET

services:
  # Backend-for-Frontend (BFF) - Handles raffstore commands and events
  bff:
    container_name: knx-iot-bff
    build:
      context: .
      dockerfile: docker/Dockerfile.bff
    environment:
      - PORT=3000
      - NODE_ENV=${NODE_ENV:-production}
      - GATEWAY_URL=${GATEWAY_URL}
      - CLIENT_ID=${CLIENT_ID}
      - CLIENT_SECRET=${CLIENT_SECRET}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - TZ=${TZ:-Europe/Vienna}
      # Syslog Configuration (for UDP forwarding)
      - SYSLOG_HOST=${SYSLOG_HOST}
      - SYSLOG_PORT=${SYSLOG_PORT}
      # Path of the mounted raffstore configuration file inside the container
      - RAFFSTORE_CONFIG_PATH=/app/config/raffstore-config.json
    ports:
      - "3000:3000"
    volumes:
      # Optional: override raffstore config at runtime (falls back to built-in default if missing)
      - ./config/raffstore-config.json:/app/config/raffstore-config.json:ro
    restart: unless-stopped
    networks:
      - knx-network

  # Angular Frontend
  frontend:
    container_name: knx-iot-frontend
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment:
      # Use host timezone for logging consistency
      - TZ=${TZ:-Europe/Vienna}
    ports:
      - "8080:80"
    depends_on:
      - bff
    restart: unless-stopped
    networks:
      - knx-network

networks:
  knx-network:
    driver: bridge
